package com.bd.tracker.core.repository;

import com.bd.tracker.core.entity.ScrapInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScrapInfoRepository extends JpaRepository<ScrapInfo, Long> {
}
